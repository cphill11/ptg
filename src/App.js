// App.js is the center of the application (root component)

// to effect change on the application, we need to either modify this file or add components inside of it

import React from "react";
import WrapperContainer from "./components/Wrapper-container";

const App = () => <WrapperContainer />;

export default App;
